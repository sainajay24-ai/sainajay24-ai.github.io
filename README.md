# Ajay Sain Portfolio — V7

V7 adds a floating, glass-style top navigation bar:
- fixed while scrolling
- rounded pill shape
- subtle shadow and backdrop blur
- responsive mobile behaviour
- extra hero spacing so the navigation does not overlap the introduction

The portfolio remains a standalone `index.html` with embedded CSS and requires no build system.
